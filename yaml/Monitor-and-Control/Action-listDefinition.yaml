asyncapi: 3.0.0
info:
  title: Action Service listDefinition API
  version: 1.0.0
  description: This API allows clients to interact with the listDefinition iteraction of the Action Service.
defaultContentType: application/json
servers:
  production:
    host: 'localhost:{port}'
    protocol: mqtt
    description: MQTT server for the listDefinition interaction.
    variables:
      port:
        enum:
          - '8883'
          - '8884'
        default: '8883'
    bindings:
      mqtt:
        clientId: guest
        cleanSession: false
        keepAlive: 0
        lastWill:
          topic: /will
          qos: 0
          message: Guest gone offline.
          retain: false
channels:
  Send_Action_listDefinition:
    address: Send_Action_listDefinition
    messages:
      Action.listDefinition_Send.message:
        $ref: '#/components/messages/Action_listDefinition_Send'
    description: >
      Send a **Action_listDefinition_Send** message in this channel to receive a
      **Action_listDefinition_Receive** message over the **Receive_Action_listDefinition** channel.
  Receive_Action_listDefinition:
    address: Receive_Action_listDefinition
    messages:
      Action.listDefinition_Receive.message:
        $ref: '#/components/messages/Action_listDefinition_Receive'
    description: >
      Use this channel to receive Action listDefinition responses as **Action_listDefinition_Receive**
      messages.
  Error_Action_listDefinition:
    address: Error_Action_listDefinition
    messages:
      Action.listDefinition_Error.message:
        $ref: '#/components/messages/Action_listDefinition_Error'
    description: >
      Use this channel to receive Action listDefinition errors as **Action_listDefinition_ReceiveErrors**
      messages.
operations:
  Action_listDefinition_Send:
    action: send
    channel:
      $ref: '#/channels/Send_Action_listDefinition'
    messages:
      - $ref: '#/channels/Send_Action_listDefinition/messages/Action.listDefinition_Send.message'
  Action_listDefinition_Receive:
    action: receive
    channel:
      $ref: '#/channels/Receive_Action_listDefinition'
    messages:
      - $ref: '#/channels/Receive_Action_listDefinition/messages/Action.listDefinition_Receive.message'
  Action_listDefinition_Error:
    action: receive
    channel:
      $ref: '#/channels/Error_Action_listDefinition'
    messages:
      - $ref: '#/channels/Error_Action_listDefinition/messages/Action.listDefinition_Error.message'
components:
  schemas:
    Action_listDefinition_Send:
      type: object
      properties:
        transactionId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        actionNames:
          type: array
          items:
            type: string
          description: |
            The actionNames field shall contain a list of action names to retrieve the ActionIdentity and ActionDefinition object instance identifiers for.
            The request may contain the wildcard value of '*' to return all supported ActionIdentity and ActionDefinition objects.
            The wildcard value should be checked for first, if found no other checks of supplied identifiers shall be made.
            If a provided identifier does not include a wildcard and does not match an existing ActionIdentity object then this operation shall fail with an UNKNOWN error.
    Action_listDefinition_Receive:
      type: object
      properties:
        transactionId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        actionInstIds:
          $ref: '#/components/schemas/mc/ObjectInstancePair'
          description: |
            The response shall contain a list of matching ActionIdentity and ActionDefinition object instance identifiers.
            The returned list shall maintain the same order as the submitted list unless the wildcard value was included in the request.
    Action_listDefinition_Error:
      type: object
      properties:
        transactionId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        area:
          type: string
          description: The area in which the error applies.
          enum:
            - MAL
        name:
          type: string
          description: A code representing the error.
          enum:
            - UNKNOWN
        extraInformation:
          type: array
          items:
            type: integer
            format: uint32
            description: A list of the indexes of the error values shall be contained in the extra information field.
    mc:
      ObjectInstancePair:
        properties:
          objDefInstanceId:
            description: The object instance identifier of the Definition object.
            format: int64
            type: integer
          objIdentityInstanceId:
            description: The object instance identifier of the Identity object.
            format: int64
            type: integer
        type: object
  messages:
    Action_listDefinition_Send:
      description: Action listDefinition request submission
      payload:
        $ref: '#/components/schemas/Action_listDefinition_Send'
    Action_listDefinition_Receive:
      description: Action listDefinition update response
      payload:
        $ref: '#/components/schemas/Action_listDefinition_Receive'
    Action_listDefinition_Error:
      description: Action listDefinition error response
      payload:
        $ref: '#/components/schemas/Action_listDefinition_Error'
