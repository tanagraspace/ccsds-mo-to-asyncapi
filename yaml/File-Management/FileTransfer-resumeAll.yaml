asyncapi: 3.0.0
info:
  title: FileTransfer Service resumeAll API
  version: 1.0.0
  description: This API allows clients to interact with the resumeAll interaction of the FileTransfer Service.
defaultContentType: application/json
servers:
  production:
    host: 'localhost:{port}'
    protocol: mqtt
    description: MQTT server for the FileTransfer Service's resumeAll interaction.
    variables:
      port:
        default: '8883'
channels:
  submit_FileTransfer_resumeAll:
    address: submit_FileTransfer_resumeAll
    messages:
      FileTransfer.resumeAll_submit.message:
        $ref: '#/components/messages/FileTransfer_resumeAll_submit'
    description: >
      Send a **FileTransfer_resumeAll_submit** message in this channel to receive a
      **FileTransfer_resumeAll_None** message over the **None_FileTransfer_resumeAll** channel.
  error_FileTransfer_resumeAll:
    address: error_FileTransfer_resumeAll
    messages:
      FileTransfer.resumeAll_error.message:
        $ref: '#/components/messages/FileTransfer_resumeAll_error'
    description: >
      Use this channel to receive FileTransfer resumeAll errors as **FileTransfer_resumeAll_NoneErrors**
      messages.
operations:
  FileTransfer_resumeAll_submit:
    action: send
    channel:
      $ref: '#/channels/submit_FileTransfer_resumeAll'
    messages:
      - $ref: '#/channels/submit_FileTransfer_resumeAll/messages/FileTransfer.resumeAll_submit.message'
  FileTransfer_resumeAll_error:
    action: receive
    channel:
      $ref: '#/channels/error_FileTransfer_resumeAll'
    messages:
      - $ref: '#/channels/error_FileTransfer_resumeAll/messages/FileTransfer.resumeAll_error.message'
components:
  schemas:
    FileTransfer_resumeAll_submit:
      type: object
      properties:
        sequenceId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        entityId:
          type: string
        window:
          $ref: '#/components/schemas/fm/file_transfer/FileTransferWindow'
          description: |
            The Transmit (Tx) and/or Receive (Rx) windows to be closed.
    FileTransfer_resumeAll_error:
      type: object
      properties:
        sequenceId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        area:
          type: string
          description: The area in which the error applies.
          enum:
            - FM
        name:
          type: string
          description: A code representing the error.
          enum:
            - UNKNOWN_ENTITY
            - CANNOT_SET_MIB_VALUE
            - OTHER_ERROR
        extraInformation:
          type: array
          items:
            type: integer
            format: uint32
            description: A list of the indexes of the error values shall be contained in the extra information field.
    fm:
      file_transfer:
        FileTransferWindow:
          description: This enumeration represents the communication windows of a file transfer
            entity. It should be noted that the reception/transmission is from the point of
            view of the entity being addressed.
          enum:
          - TX_WINDOW
          - RX_WINDOW
          - BOTH_WINDOW
          type: string
  messages:
    FileTransfer_resumeAll_submit:
      description: FileTransfer resumeAll request submission
      payload:
        $ref: '#/components/schemas/FileTransfer_resumeAll_submit'
    FileTransfer_resumeAll_error:
      description: FileTransfer resumeAll error response
      payload:
        $ref: '#/components/schemas/FileTransfer_resumeAll_error'
