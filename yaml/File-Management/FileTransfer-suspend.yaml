asyncapi: 3.0.0
info:
  title: FileTransfer Service suspend API
  version: 1.0.0
  description: This API allows clients to interact with the suspend interaction of the FileTransfer Service.
defaultContentType: application/json
servers:
  production:
    host: 'localhost:{port}'
    protocol: mqtt
    description: MQTT server for the FileTransfer Service's suspend interaction.
    variables:
      port:
        default: '8883'
channels:
  submit_FileTransfer_suspend:
    address: submit_FileTransfer_suspend
    messages:
      FileTransfer.suspend_submit.message:
        $ref: '#/components/messages/FileTransfer_suspend_submit'
    description: >
      Send a **FileTransfer_suspend_submit** message in this channel to receive a
      **FileTransfer_suspend_None** message over the **None_FileTransfer_suspend** channel.
  error_FileTransfer_suspend:
    address: error_FileTransfer_suspend
    messages:
      FileTransfer.suspend_error.message:
        $ref: '#/components/messages/FileTransfer_suspend_error'
    description: >
      Use this channel to receive FileTransfer suspend errors as **FileTransfer_suspend_NoneErrors**
      messages.
operations:
  FileTransfer_suspend_submit:
    action: send
    channel:
      $ref: '#/channels/submit_FileTransfer_suspend'
    messages:
      - $ref: '#/channels/submit_FileTransfer_suspend/messages/FileTransfer.suspend_submit.message'
  FileTransfer_suspend_error:
    action: receive
    channel:
      $ref: '#/channels/error_FileTransfer_suspend'
    messages:
      - $ref: '#/channels/error_FileTransfer_suspend/messages/FileTransfer.suspend_error.message'
components:
  schemas:
    FileTransfer_suspend_submit:
      type: object
      properties:
        sequenceId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        transactionId:
          $ref: '#/components/schemas/fm/file_transfer/FileTransactionId'
    FileTransfer_suspend_error:
      type: object
      properties:
        sequenceId:
          type: string
          description: A unique identifier to map the response (receive message) to the request (send message). If no request message exists then this unique identifier can be used to track the sequence order of the received messages.
        area:
          type: string
          description: The area in which the error applies.
          enum:
            - FM
        name:
          type: string
          description: A code representing the error.
          enum:
            - UNKNOWN_TRANSACTION
            - OTHER_ERROR
        extraInformation:
          type: array
          items:
            type: integer
            format: uint32
            description: A list of the indexes of the error values shall be contained in the extra information field.
    fm:
      file_transfer:
        FileTransactionId:
          properties:
            entity:
              description: ''
              format: binary
              type: string
            id:
              description: The ID as a byte array
              format: binary
              type: string
          type: object
  messages:
    FileTransfer_suspend_submit:
      description: FileTransfer suspend request submission
      payload:
        $ref: '#/components/schemas/FileTransfer_suspend_submit'
    FileTransfer_suspend_error:
      description: FileTransfer suspend error response
      payload:
        $ref: '#/components/schemas/FileTransfer_suspend_error'
